// PAGINATION
function paginationRenderBulletConfig(_index: number, className: string): string {
    return `<span class="${className} bg-swiper-pagination-bullet w-3 h-3"></span>`;
}

export {
    paginationRenderBulletConfig,
};
